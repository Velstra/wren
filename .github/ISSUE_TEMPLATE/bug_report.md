---
name: Bug report
about: Something in Wren behaves incorrectly
title: ""
labels: bug
---

**What happened**
A clear description of the bug.

**What you expected**
What should have happened instead.

**Affected component**
Crate / protocol (e.g. `wren-bgp`, OSPFv2 runner) and the version or commit.

**Minimal config & topology**
The smallest `wren.toml` (and veth/namespace layout) that reproduces it.

```toml
# config here
```

**Steps to reproduce**
1. ...
2. ...

**Logs / output**
Relevant `wren` log lines, `wren show ...` output, or `ip route` state.

**Environment**
- Wren version / commit:
- OS / kernel:
- Rust version (`rustc --version`):
